export enum ETaskStatus {
  ACTIVE = 'active',
  FINISHED = 'finished',
}

export enum EFilterByOrder {
  NEW = 'new',
  OLD = 'old',
}

export enum EFilterByStatus {
  ALL = 'all',
  ACTIVE = 'active',
  FINISHED = 'finished',
}
